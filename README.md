# Hoarder

Hoarder is a local-first connector platform for one-way data aggregation.

The first release syncs OpenDAL-backed sources into a local vault, records state
in SQLite through SeaORM, and exposes a local Axum API with a Svelte management
console.
